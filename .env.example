# === Backend Required ===
DATABASE_URL=postgresql://user:password@host:port/dbname
# Or discrete PG vars if not using DATABASE_URL
# PGHOST=
# PGPORT=5432
# PGUSER=
# PGPASSWORD=
# PGDATABASE=
PGSSL=true

OPENAI_API_KEY=sk-...
GOOGLE_AI_API_KEY=AIza...
JWT_SECRET=replace_with_24+_char_secret
CORS_ORIGIN=*

# Optional: override port locally
PORT=5000

# === Frontend Required ===
# Update frontend/src/config.js API_BASE_URL before deploy
# No VITE_* vars currently in use
